# External validation.

source("R/preprocessing.R")
source("R/model_smo_svm.R")
source("R/metrics.R")

# TODO:
# Train final frozen pipeline on discovery cohort only.
# Project onto external cohort(s).
# Evaluate once with no tuning or threshold adjustment.
