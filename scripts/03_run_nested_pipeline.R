# Run guarded nested SMO/SVM pipeline for pilot.

source("R/pipelines.R")

# TODO:
# Load discovery expression matrix and labels.
# Create outer folds with fixed seed.
# Run inner-loop feature selection and SMO/SVM tuning.
# Export predictions, selected features, hyperparameters, and fold IDs.
